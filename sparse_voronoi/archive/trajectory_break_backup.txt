    // if (tot_steps + state.edge_steps >= prediction_horizon_-1)
    // {
    //   ROS_INFO_STREAM("voronoi path stopped after exceeding prediction horizon");
    //   // Define number of steps left in prediction horizon.
    //   state.edge_steps = prediction_horizon_-1 - tot_steps;
    //   state.next_state = next_state;
    //   waypoint.pose.position = next_state.pos;
    //   voro_path_.poses.push_back(waypoint);

    //   tot_steps += state.edge_steps;
    //   path_dist += state.edge_length;

    //   break;
    // }
