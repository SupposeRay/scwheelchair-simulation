# SLAM tools

- Start SLAM, e.g.:

    - $ roslaunch scat_launch slam_mode.launch

- Drive around for a while.

- Save the map:

    - $ rosrun map_server map_saver [--occ <threshold_occupied>] [--free <threshold_free>] [-f <mapname>]
    - Example: $ rosrun map_server map_saver -f rris2
